# ── AI (required) ────────────────────────────────────
OPENAI_API_KEY=sk-...                # Quality-critical routes (generate-all-answers, generate-spec)
GROQ_API_KEY=gsk_...                 # Speed-critical routes (analyze-project, generate-answer, generate-project-description)
                                     # Optional — falls back to OpenAI if missing

# ── Analytics ─────────────────────────────────────────
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ── Site ──────────────────────────────────────────────
NEXT_PUBLIC_BASE_URL=https://specifythat.com

# ── Email (nodemailer → Gmail SMTP) ──────────────────
GMAIL_USER=you@gmail.com
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx   # App password, NOT your Gmail password
FEEDBACK_TO=you@gmail.com                # Defaults to GMAIL_USER if blank

# ── Resend (email list) ───────────────────────────────
# Optional — stores newsletter signups as global Contacts
# Each project gets its own Segment in the shared Resend team
# resend.com → Segments → create "SpecifyThat" → copy ID
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_SEGMENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
